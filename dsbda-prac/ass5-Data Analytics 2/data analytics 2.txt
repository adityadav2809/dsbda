# Importing necessary libraries
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import confusion_matrix, accuracy_score, precision_score, recall_score, f1_score
# Step 1: Load the dataset
print("Step 1: Load the Dataset")
df = pd.read_csv('Social_Network_Ads.csv')
# Display the first few rows of the dataset
print(df.head())
print("\n")
# Step 2: Preprocess the data
print("Step 2: Preprocess the Data")
# Checking for any missing values
missing_values = df.isnull().sum()
print(f"Missing values in each column:\n{missing_values}\n")

# Splitting data into features (X) and target (y)
X = df[['Age', 'EstimatedSalary']].values  # Features: Age and Estimated Salary
y = df['Purchased'].values  # Target variable: Purchased (1 or 0)

print("Feature data (X):")
print(X[:5])  # First 5 rows of feature data
print("\nTarget data (y):")
print(y[:5])  # First 5 rows of target data
print("\n")
# Step 3: Splitting data into training and test sets
print("Step 3: Splitting data into training and test sets")

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.25, random_state=42)

print(f"Training data size: {X_train.shape[0]} samples")
print(f"Test data size: {X_test.shape[0]} samples\n")
# Step 4: Train the Logistic Regression model
print("Step 4: Train the Logistic Regression Model")
model = LogisticRegression()
model.fit(X_train, y_train)
print("Model trained successfully!\n")
# Step 5: Make predictions
print("Step 5: Make Predictions")
y_pred = model.predict(X_test)
# Step 6: Compute confusion matrix
print("Step 6: Compute Confusion Matrix")
cm = confusion_matrix(y_test, y_pred)
print("Confusion Matrix:")
print(cm)
print("\n")
# Step 7: Compute accuracy, error rate, precision, and recall
print("Step 7: Compute Performance Metrics")

# Calculate accuracy, error rate, precision, recall, and F1-score
accuracy = accuracy_score(y_test, y_pred)  # Proportion of correctly classified samples
error_rate = 1 - accuracy  # Proportion of misclassified samples
precision = precision_score(y_test, y_pred)  # Proportion of true positives among all predicted positives
recall = recall_score(y_test, y_pred)  # Proportion of true positives among all actual positives
f1 = f1_score(y_test, y_pred)  # Harmonic mean of precision and recall

print(f"Accuracy: {accuracy:.4f}")
print(f"Error Rate: {error_rate:.4f}")
print(f"Precision: {precision:.4f}")
print(f"Recall: {recall:.4f}")
print(f"F1-Score: {f1:.4f}")



### **General Understanding of Logistic Regression and Classification**
1. **What is Logistic Regression, and why is it used in this project?**
   - Explain the concept of Logistic Regression and why it is suitable for binary classification tasks.

2. **Can you describe the difference between Logistic Regression and Linear Regression?**
   - How does Logistic Regression work for classification, while Linear Regression is used for continuous output?

3. **Why are we using `Age` and `EstimatedSalary` as features in this dataset?**
   - How did you decide which features to use in the model?

4. **What is the target variable in this dataset, and what does it represent?**
   - Can you explain what the values in the `Purchased` column represent?

---

### **Data Preprocessing**
5. **What preprocessing steps did you perform on the dataset?**
   - Did you handle missing values? Why or why not?

6. **What is the importance of splitting the data into features (`X`) and target (`y`)?**
   - Explain the role of each and why it's necessary to separate them.

7. **Why is it important to check for missing values in the dataset?**
   - How would missing data impact model performance, and how could you handle it if it existed?

8. **Why did you choose to work with `Age` and `EstimatedSalary` as input features?**
   - Are these the most relevant features for predicting the target variable `Purchased`?

---

### **Train-Test Split**
9. **Why did you split the dataset into training and test sets?**
   - How do you ensure that the model generalizes well and doesn’t overfit to the training data?

10. **What does the `test_size=0.25` parameter mean when using `train_test_split()`?**
    - Why did you choose to split the data in this way? Can you explain the impact of using different test sizes?

11. **What does the `random_state=42` argument do?**
    - Why is setting a random seed important in machine learning?

---

### **Logistic Regression Model**
12. **How does the Logistic Regression model work for binary classification?**
    - Can you explain the decision boundary in logistic regression?

13. **What does the `fit()` function do in the context of the `LogisticRegression` model?**
    - What is happening when the model is trained using `model.fit()`?

14. **What are the coefficients learned by the Logistic Regression model?**
    - Can you explain how the model assigns weights to features during training?

15. **How would you interpret the coefficients of a Logistic Regression model for this dataset?**
    - How would you interpret a positive or negative coefficient in terms of feature importance?

---

### **Model Evaluation**
16. **What is a Confusion Matrix, and why is it useful?**
    - Can you explain the components of the confusion matrix: True Positives (TP), False Positives (FP), True Negatives (TN), and False Negatives (FN)?

17. **What do you learn from the confusion matrix in your results?**
    - What does the matrix tell you about the model’s performance?

18. **What is the difference between accuracy and error rate?**
    - Can you explain why these two metrics are related but measure different aspects of the model?

19. **What does Precision mean, and how is it calculated?**
    - Can you explain how Precision is different from Recall?

20. **What does Recall mean, and why is it important?**
    - Why might Recall be more important than Precision in certain scenarios?

21. **Why do we use F1-Score, and how is it calculated?**
    - Can you explain the tradeoff between Precision and Recall and how the F1-Score addresses this?

22. **When would you prefer to use Precision over Recall or vice versa?**
    - Can you give a real-world example where Precision would be more important than Recall?

---

### **Advanced Concepts**
23. **What is overfitting, and how can it affect the performance of the Logistic Regression model?**
    - How would you prevent overfitting in a logistic regression model?

24. **What is the significance of regularization in Logistic Regression?**
    - Can you explain L1 and L2 regularization, and why they might be important in your model?

25. **Can Logistic Regression be used for multi-class classification? If so, how would you extend it?**
    - What techniques are used to handle multiple classes in Logistic Regression?

26. **If the dataset had more features, how would you handle feature selection or dimensionality reduction?**
    - Would you apply techniques like PCA or feature selection methods?

27. **What other classification algorithms could you have used besides Logistic Regression?**
    - Can you explain why you chose Logistic Regression over models like Decision Trees, Random Forests, or Support Vector Machines?

28. **What are some common problems when using Logistic Regression, and how do you troubleshoot them?**
    - What would you do if your Logistic Regression model performs poorly on the test set?

---

### **Model Improvement**
29. **What steps would you take to improve the performance of the Logistic Regression model?**
    - Would you consider adding more features, tuning hyperparameters, or using different preprocessing techniques?

30. **What are the potential limitations of using Logistic Regression for this dataset?**
    - Can you suggest any improvements or alternative models to address these limitations?

---

### **Real-World Application**
31. **Can you think of a real-world scenario where Logistic Regression could be applied effectively?**
    - Give an example of binary classification problems where this algorithm could be useful.

32. **How would you interpret the model’s output (probabilities) in a real-world scenario?**
    - What would you do if the model provides a probability close to 0.5 for a prediction?

---
